JNDI-->DataSource-->Connection-->连接池

entity

service包：
  **Service:接口  <--业务逻辑

Dao (接口) -->DaoImpl(接口实现类) :数据库的数据操作
一般一个Dao对应一张数据库表 （********）


删除新闻分类    
    查询某个新闻分类下是否有新闻
     有--》提示无法删除
     无--》删除


NewsCategoryDao   -->删除新闻分类deleteCategory(NewsCategory category);
    NewsCategoryDaoImpl实现    deleteCategory(NewsCategory category){ delete from ....  }

NewsDao ->查询某个新闻分类下是否有新闻? getNewsCountByCategory(NewsCategory category);
    NewsDaoImpl 实现
		SELECT COUNT(1) FROM news_detail
		WHERE categoryid = ?

删除新闻分类     
	需要调用两个Dao的方法，配合工作

int count = NewsDao.getNewsCountByCategory(category);
if（ count ==0）{
	NewsCategoryDao.deleteCategory(category);
}else{
	System.out.println("无法删除该分类！");
}

如上代码写在哪里？--》NewsCategoryService

Service 与dao的区别？
    dao 数据操作，一般一个dao对应操作一张数据表
    Service 业务逻辑
    Service去调用dao
程序的结构，每层做的事情？




新闻列表显示：
 id   title   author   createDate


将新闻数据封装成新闻对象（JavaBean），将新闻对象放进一个集合，将集合扔给页面







class="admin-list-td-h2"  使新闻数据隔行变色的原因

mysql--》DQL--》limit















